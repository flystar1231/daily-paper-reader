---
title: "Action Agent: Agentic Video Generation Meets Flow-Constrained Diffusion"
title_zh: Action Agent：智能体视频生成与流约束扩散控制的结合
authors: "Jeffrin Sam, Nguyen Khang, Yara Mahmoud, Miguel Altamirano Cabrera, Dzmitry Tsetserukou"
date: 2026-05-02
pdf: "https://arxiv.org/pdf/2605.01477v1"
tags: ["query:cv-vg-cs"]
score: 8.0
evidence: 使用流约束扩散的可控视频生成
tldr: Action Agent是一个两阶段机器人导航框架，将智能体视频生成与流约束扩散控制相结合。第一阶段利用大语言模型作为编排器，通过迭代验证和跨任务记忆生成高质量的第一视角导航视频；第二阶段通过FlowDiT模型将视频转化为连续速度指令。该方法在多种机器人形态（人形、无人机、轮式）上实现了高效的语言引导导航，显著提升了生成成功率和实机任务完成率。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.01477v1/fig-001.webp\", \"caption\": \"Fig. 2: Stage I: agentic trajectory imagination (digital rehearsal). A central LLM agent orchestrates (i) a vision–language model for prompt construction, (ii) a text-to-video diffusion generator, and (iii) a reasoning-based evaluator that scores candidate videos and produces structured critiques. The agent iteratively refines prompts using feedback and memory until a validated reference navigation video Vgoal is obtained.\", \"page\": 4, \"index\": 1, \"width\": 948, \"height\": 266}, {\"url\": \"assets/figures/arxiv/2605.01477v1/fig-002.webp\", \"caption\": \"Fig. 3: Stage II: FlowDiT execution module. FlowDiT conditions on the validated reference video Vgoal (visual features and motion cues) and the instruction L to predict a horizon-H sequence of velocity commands (vx, vy, ω). The policy executes in a recedinghorizon manner by applying the first action and re-evaluating at the next timestep (with optional live observation It when available).\", \"page\": 4, \"index\": 2, \"width\": 514, \"height\": 471}, {\"url\": \"assets/figures/arxiv/2605.01477v1/fig-003.webp\", \"caption\": \"Fig. 1: Action Agent system overview. Stage I performs agentic trajectory imagination by generating and validating a first-person visual intermediate representation (reference navigation video) from a language instruction and an initial observation. Stage II executes this reference using FlowDiT to produce continuous velocity commands for deployment across multiple robot embodiments.\", \"page\": 1, \"index\": 3, \"width\": 462, \"height\": 479}]"
motivation: 旨在解决多形态机器人导航中，如何将抽象的语言指令有效转化为符合物理规律的视觉轨迹及精确控制指令的挑战。
method: 提出一种解耦架构，结合LLM驱动的视频生成编排（Stage I）与基于流约束扩散变换器FlowDiT的动作预测（Stage II）。
result: "视频生成成功率从35%提升至86%，并在真实Unitree G1人型机器人上实现了64.7%的开环导航任务完成率。"
conclusion: 实验证明将轨迹想象与动作执行解耦的范式，为多形态机器人提供了一种可扩展且具备形态感知能力的语言引导导航方案。
---

## 摘要
我们提出了 Action Agent，这是一个两阶段框架，将智能体导航视频生成与流约束扩散控制相结合，用于多具身机器人导航。在第一阶段，大语言模型（LLM）作为编排模块，负责选择视频扩散模型、通过迭代验证优化提示词，并积累跨任务记忆，从而根据语言和图像输入合成符合物理规律的第一人称导航视频。在 50 个导航任务中，这使视频生成的成功率从 35%（单次尝试）提升至 86%。在第二阶段，我们引入了 FlowDiT，这是一种流约束扩散 Transformer（Flow-Constrained Diffusion Transformer），它利用动作空间去噪扩散将优化后的目标视频和语言指令转换为连续的速度指令。FlowDiT 集成了 DINOv2 视觉特征、用于自我运动表示的学习光流，以及用于语义停止的 CLIP 语言嵌入。我们在 RECON 户外导航数据集上进行预训练，并在 Isaac Sim 中采集的 203 个 Unitree G1 人形机器人片段上进行微调，以校准速度动力学。一个仅含 4300 万参数的检查点在仿真中实现了 73.2% 的导航成功率，在开环执行下，在未见过的室内环境中实现了真实 Unitree G1 机器人 64.7% 的任务完成率，同时运行频率达到 40-47 Hz。我们在三种具身形态上评估了 Action Agent：Unitree G1 人形机器人（真实硬件）、无人机和轮式移动机器人（Isaac Sim），证明了将轨迹想象与执行解耦为语言引导导航提供了一种可扩展且具身感知的范式。

## Abstract
We present Action Agent, a two-stage framework that unifies agentic navigation video generation with flow-constrained diffusion control for multi-embodiment robot navigation. In Stage I, a large language model (LLM) acts as an orchestration module that selects video diffusion models, refines prompts through iterative validation, and accumulates cross-task memory to synthesize physically plausible first-person navigation videos from language and image inputs. This increases video generation success from 35% (single-shot) to 86% across 50 navigation tasks. In Stage II, we introduce FlowDiT, a Flow-Constrained Diffusion Transformer that converts optimized goal videos and language instructions into continuous velocity commands using action-space denoising diffusion. FlowDiT integrates DINOv2 visual features, learned optical flow for ego-motion representation, and CLIP language embeddings for semantic stopping. We pretrain on the RECON outdoor navigation dataset and fine-tune on 203 Unitree G1 humanoid episodes collected in Isaac Sim to calibrate velocity dynamics. A single 43M-parameter checkpoint achieves 73.2% navigation success in simulation and 64.7% task completion on a real Unitree G1 in unseen indoor environments under open-loop execution, while operating at 40--47 Hz. We evaluate Action Agent across three embodiments: a Unitree G1 humanoid (real hardware), a drone, and a wheeled mobile robot (Isaac Sim), demonstrating that decoupling trajectory imagination from execution yields a scalable and embodiment-aware paradigm for language-guided navigation.

---

## 论文详细总结（自动生成）

这是一份关于论文《Action Agent: Agentic Video Generation Meets Flow-Constrained Diffusion》的深度结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
论文旨在解决**具身智能（Embodied AI）中语言引导导航**的难题。传统的导航方法（如 SLAM + 路径规划）在环境变化时鲁棒性不足，而端到端的视觉-语言-动作（VLA）模型虽然强大，但往往将高层意图与底层控制混为一谈，导致“黑盒”化、计算开销巨大且难以在边缘设备上高频运行。
**核心动机**：提出一种“先想象、后执行”的解耦架构。通过将导航任务分解为“视觉中间表示（VIR）生成”和“基于流约束的动作执行”，使机器人能先在像素空间“排练”路径，再转化为具体的物理指令。

### 2. 方法论：核心思想与关键技术
Action Agent 框架分为两个阶段：

*   **第一阶段：智能体轨迹想象（Digital Rehearsal）**
    *   **核心思想**：利用 LLM 作为编排器，驱动视频生成模型合成第一视角的导航视频。
    *   **关键技术**：
        *   **迭代优化环路**：LLM 协调视觉语言模型（Qwen3-VL）构建提示词，视频模型（WAN 2.2 或 LTX-Video）生成视频，推理验证器（Cosmos-Reason1）根据提示词遵循度、物理合理性和视觉质量进行打分。
        *   **三层记忆架构**：包含静态规则、长期记忆（跨任务成功策略）和短期记忆（单任务迭代历史），用于减少生成幻觉。
*   **第二阶段：流约束扩散变换器（FlowDiT）**
    *   **核心思想**：将第一阶段生成的视频转化为连续的速度指令 $(v_x, v_y, \omega)$。
    *   **关键技术**：
        *   **多模态条件嵌入**：融合了 DINOv2 提取的视觉特征、专门学习的**光流（Optical Flow）特征**（用于解决深度歧义）以及 CLIP 语言嵌入（用于语义停止）。
        *   **扩散策略**：采用 Diffusion Transformer (DiT) 架构，通过去噪过程预测未来 8 帧的动作序列，支持 40-47 Hz 的高频闭环控制。

### 3. 实验设计
*   **数据集与场景**：
    *   **预训练**：使用 Open X-Embodiment 中的 RECON 户外导航数据集（11,830 个片段）。
    *   **微调**：在 Isaac Sim 仿真环境（仓库、医院走廊）中采集的 203 个 Unitree G1 人形机器人片段。
    *   **实机测试**：在未见过的室内实验室/办公环境中对 Unitree G1 进行 17 次实机测试。
*   **Benchmark 与对比方法**：
    *   对比了 **ViNT**（视觉 Transformer 导航）、**NoMaD**（目标条件扩散导航）和 **OpenVLA**（大型视觉-语言-动作模型）。
    *   评估指标包括：视频生成的 Pass Rate、导航成功率（SR）、平均轨迹误差（ATE）和方向准确率（DA）。

### 4. 资源与算力
*   **硬件**：训练和推理均使用了 **NVIDIA RTX 5090** GPU。
*   **模型规模**：FlowDiT 仅有 **43M（4300万）** 可训练参数，相比 7B 规模的 OpenVLA 实现了 **161 倍** 的参数量缩减。
*   **效率**：推理延迟约 20ms/步，支持 40-47 Hz 的实时运行。
*   **训练时长**：文中未明确给出具体的训练总时长，但提到了微调阶段的 Batch Size 为 8，采用 FP16 精度。

### 5. 实验数量与充分性
*   **实验规模**：
    *   针对第一阶段，在 50 个复杂导航任务上进行了验证。
    *   针对第二阶段，在三种具身形态（人形机器人、无人机、轮式机器人）上进行了跨平台测试。
    *   进行了**消融实验**，分别验证了光流特征、语言嵌入和视觉特征对导航性能的贡献。
*   **充分性评价**：实验设计较为全面，涵盖了从仿真到实机的迁移（Sim-to-Real），且对比了当前最前沿的 VLA 模型。通过跨形态（空中、地面、足式）的测试，有力证明了该方法的通用性。

### 6. 主要结论与发现
*   **智能体化生成的优势**：通过 LLM 迭代优化，视频生成成功率从单次尝试的 35% 提升至 **86%**。
*   **轻量化模型的高效性**：43M 参数的 FlowDiT 在仿真中达到 73.2% 的成功率，优于部分参数量更大的基准模型。
*   **光流的重要性**：消融实验显示，加入光流特征使方向准确率提升了 8.3%，有效解决了视觉导航中的“缩放/深度歧义”问题。
*   **实机可行性**：在开环执行下，真实人形机器人实现了 64.7% 的任务完成率。

### 7. 优点（亮点）
*   **解耦架构**：将复杂的语义推理（视频生成）与高频的动作控制（FlowDiT）分离，既保留了生成式模型的想象力，又满足了实时控制的低延迟要求。
*   **形态感知**：通过第一阶段的验证环路，生成的视频能自动适配不同机器人的物理约束。
*   **参数极其高效**：在保持竞争力的性能下，极大地降低了对计算资源的需求，适合边缘部署。

### 8. 不足与局限
*   **开环漂移**：实机测试采用开环执行，导致 35.3% 的失败案例主要源于视频与物理尺度不匹配产生的累积误差。
*   **规划时效限制**：受限于当前视频生成技术，单次生成的视频长度仅为 5-15 秒，限制了长距离、长时间跨度的任务规划。
*   **极端环境精度**：在极窄空间（如窄门）中，系统仍难以达到厘米级的避障精度。

（完）
