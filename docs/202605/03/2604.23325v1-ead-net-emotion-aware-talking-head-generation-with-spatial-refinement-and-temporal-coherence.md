---
title: "EAD-Net: Emotion-Aware Talking Head Generation with Spatial Refinement and Temporal Coherence"
title_zh: EAD-Net：结合空间细化与时间相干性的情感感知说话人视频生成
authors: "Yahui Li, Yinfeng Yu, Liejun Wang, Shengjie Shen"
date: 2026-04-25
pdf: "https://arxiv.org/pdf/2604.23325v1"
tags: ["query:cv-vg-cs"]
score: 8.0
evidence: 使用扩散模型的情感感知谈话头像视频生成
tldr: 本研究提出EAD-Net，一种基于扩散模型的表情感知说话人视频生成网络。针对现有方法情感语义不足、长视频全局运动感知差及口型同步退化等问题，该模型引入大语言模型提取的高级语义指导，并结合时空定向注意力与时间帧图推理模块。实验证明，该方法在HDTF和MEAD数据集上实现了更优的口型准确度、时间一致性和情感表达力。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.23325v1/fig-001.webp\", \"caption\": \"\", \"page\": 3, \"index\": 1, \"width\": 1788, \"height\": 256}]"
motivation: 现有情感说话人视频生成方法存在情感语义缺失、长视频时空依赖建模困难以及口型同步与情感表达难以兼顾的问题。
method: 提出包含SyncNet监督、时空定向注意力（STDA）和时间帧图推理模块（TFRM）的扩散网络，并利用大语言模型增强情感语义控制。
result: 在HDTF和MEAD数据集上的实验结果表明，该方法在口型同步精度、视频时间连贯性和情感表达准确性方面均优于现有主流模型。
conclusion: EAD-Net通过创新的时空建模与多模态融合策略，有效提升了情感说话人视频生成的质量与表现力。
---

## 摘要
情感说话人视频生成旨在生成具有准确口型同步和情感面部表情的富有表现力的肖像视频。目前的方法依赖于简单的情感标签，导致语义信息不足。虽然引入高层语义可以增强表现力，但容易导致口型同步性能下降。此外，主流生成方法在长视频中难以平衡计算效率与全局运动感知，且存在时间相干性差的问题。因此，我们提出了一种基于情感感知扩散模型的网络，称为 EAD-Net。我们引入了 SyncNet 监督和时间表示对齐（TREPA），以减轻多模态融合导致的口型同步退化。为了建模长视频序列中复杂的时空依赖关系，我们提出了一种时空定向注意力（STDA）机制，通过条带注意力捕捉全局运动模式。此外，我们设计了一个时间帧图推理模块（TFRM），通过图结构学习显式建模视频帧之间的时间相干性。为了增强情感语义控制，我们利用大语言模型从真实视频中提取文本描述，作为高层语义指导。在 HDTF 和 MEAD 数据集上的实验表明，我们的方法在口型同步准确度、时间一致性和情感准确度方面优于现有方法。

## Abstract
Emotionally talking head video generation aims to generate expressive portrait videos with accurate lip synchronization and emotional facial expressions. Current methods rely on simple emotional labels, leading to insufficient semantic information. While introducing high-level semantics enhances expressiveness, it easily causes lip-sync degradation. Furthermore, mainstream generation methods struggle to balance computational efficiency and global motion awareness in long videos and suffer from poor temporal coherence. Therefore, we propose an \textbf{E}motion-\textbf{A}ware \textbf{D}iffusion model-based \textbf{Net}work, called \textbf{EAD-Net}. We introduce SyncNet supervision and Temporal Representation Alignment (TREPA) to mitigate lip-sync degradation caused by multi-modal fusion. To model complex spatio-temporal dependencies in long video sequences, we propose a Spatio-Temporal Directional Attention (STDA) mechanism that captures global motion patterns through strip attention. Additionally, we design a Temporal Frame graph Reasoning Module (TFRM) to explicitly model temporal coherence between video frames through graph structure learning. To enhance emotional semantic control, a large language model is employed to extract textual descriptions from real videos, serving as high-level semantic guidance. Experiments on the HDTF and MEAD datasets demonstrate that our method outperforms existing methods in terms of lip-sync accuracy, temporal consistency, and emotional accuracy.

---

## 论文详细总结（自动生成）

这是一份关于论文《EAD-Net: Emotion-Aware Talking Head Generation with Spatial Refinement and Temporal Coherence》的深度结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
论文关注的是**情感感知说话人视频生成（Emotional Talking Head Generation）**。
*   **研究动机**：现有的音频驱动生成方法虽然在口型同步上取得了进展，但在情感表达上存在不足。主要挑战包括：
    1.  **语义缺失**：传统方法依赖简单的离散情感标签（如“开心”、“生气”），难以捕捉微妙的情感动态。
    2.  **性能权衡**：引入高层语义往往会干扰底层的口型同步精度。
    3.  **时空建模困难**：在处理长视频时，现有模型难以平衡计算效率与全局运动感知，导致视频出现闪烁、伪影或身份漂移。
*   **核心目标**：在保证高精度口型同步的同时，实现丰富的、时间连贯的情感表达。

### 2. 方法论：核心思想与关键技术
EAD-Net 基于**扩散模型（Diffusion Model）**架构，以去噪 U-Net 为骨干，提出了以下核心组件：
*   **语义引导的情感控制**：利用大语言模型（Qwen-VLM）从视频中提取精细的文本描述，通过 T5 编码器转化为文本嵌入，并与音频特征融合。这种“文本+音频”的双模态输入解决了音频节奏在表达情感时的歧义性。
*   **时空定向注意力（STDA）**：为了降低标准自注意力机制的平方级计算开销，设计了水平和垂直方向的“条带注意力”（Strip Attention）。它通过轻量级的卷积和池化操作实现线性复杂度的全局感受野，增强了像素级的长程依赖建模。
*   **时间帧图推理模块（TFRM）**：将视频序列建模为链式图结构，利用图神经网络（GNN）显式约束相邻帧之间的运动轨迹。这补偿了注意力机制在建模连续运动平滑性方面的不足。
*   **多重损失函数监督**：结合了噪声预测损失（$L_{noise}$）、基于 SyncNet 的口型同步损失（$L_{sync}$）、感知损失（$L_{lpips}$）以及时间表示对齐损失（$L_{trepa}$），确保生成质量。

### 3. 实验设计
*   **数据集**：
    *   **HDTF**：高分辨率说话人数据集，用于评估视觉质量和口型同步。
    *   **MEAD**：大规模多视角情感说话人数据集，包含 8 种情感及不同强度。
    *   **RAVDESS**：用于跨数据集泛化能力测试。
*   **对比方法（Benchmarks）**：包括 Wav2Lip, IP_LAP, MuseTalk, LatentSync, Hallo2, EAMM, EAT 以及 Zhao 等人的最新研究。
*   **评估指标**：FID（图像质量）、SSIM（结构相似性）、Sync_conf（口型同步置信度）、FVD（视频连贯性）以及 Acc_emo（情感分类准确率）。

### 4. 资源与算力
*   **算力说明**：论文中**未明确说明**具体的 GPU 型号、数量以及总训练时长。
*   **实现细节**：提到视频统一处理为 25 FPS，分辨率 256x256，音频采样率 16 kHz，推理时使用 20 步 DDIM 采样。

### 5. 实验数量与充分性
*   **实验规模**：
    *   在两个主流数据集（HDTF, MEAD）上进行了详尽的定量对比。
    *   针对 STDA、TFRM 和文本语义引导三个核心组件进行了**消融实验**，验证了各模块对 FID、FVD 和情感准确率的贡献。
    *   进行了**跨数据集泛化实验**，证明模型在未见过的演员和场景下依然稳健。
*   **客观性评价**：实验设计较为全面，不仅对比了传统的 GAN 模型，也对比了最新的基于扩散模型的 SOTA 方法（如 Hallo2, LatentSync），评估维度涵盖了从底层像素到高层语义的多个层面，结果具有较强的说服力。

### 6. 主要结论与发现
*   **性能领先**：EAD-Net 在口型同步准确度（Sync_conf）和时间一致性（FVD）上均达到了最优水平。
*   **情感表达力**：通过引入 LLM 生成的文本语义，模型的情感分类准确率（Acc_emo）显著提升，证明了文本作为情感先验的有效性。
*   **时空平衡**：STDA 和 TFRM 的结合有效解决了长视频生成中的伪影和抖动问题，实现了视觉真实感与动态连贯性的平衡。

### 7. 优点（亮点）
*   **创新的语义融合**：巧妙利用 VLM（视觉语言模型）将视频转化为文本描述，为扩散模型提供了比简单标签更丰富的条件引导。
*   **高效的注意力机制**：STDA 模块在保持全局感知的同时大幅降低了计算复杂度，适合处理高分辨率或长序列视频。
*   **显式时间约束**：引入图推理模块（TFRM）来处理帧间关系，比隐式的注意力权重更能保证运动的平滑性。

### 8. 不足与局限
*   **情感类别限制**：目前模型仍主要支持 8 种预定义的情感类别，缺乏对情感强度（Intensity）的连续控制。
*   **视角局限**：实验主要集中在正面视角生成，虽然 MEAD 数据集支持多视角，但论文尚未充分展示在大角度头部转动下的生成效果。
*   **算力透明度**：缺乏训练成本的详细数据，难以评估该模型在实际部署时的资源门槛。

（完）
